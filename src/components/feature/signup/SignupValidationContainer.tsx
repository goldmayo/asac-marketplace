"use client"
import React from 'react'

export default function SignupValidationContainer({ children }: { children: React.ReactNode }) {
  return <div className="flex items-center justify-between gap-2">{children}</div>
}
